# React App Template

A React app template with:

* Parcel 2
* TypeScript
* ESLint
* Prettier
* ts-jest

## Usage

    $ mkdir app && cd app
    $ npx degit ordoghl/react-app-ts
    $ yarn install
    $ yarn start


