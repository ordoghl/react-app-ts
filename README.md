# React App Template

A CRA react app template with:

* Typescript
* eslint
* prettier
* ts-jest

## Usage

    $ mkdir app && cd app
    $ npx degit ordoghl/react-app-ts
    $ yarn install
    $ yarn start


